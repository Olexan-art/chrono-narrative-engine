[build]
  command = "npm run build"
  publish = "dist"

[[edge_functions]]
  function = "bot-ssr"
  path = "/*"

[build.environment]
  NODE_VERSION = "18"

# API rewrites are handled by the bot-ssr edge function which adds auth headers.
# Do NOT add [[redirects]] for /api/* routes here — they conflict with the edge function.

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
