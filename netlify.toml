[build]
  command = "npm run build"
  publish = "dist"

[[edge_functions]]
  function = "bot-ssr"
  path = "/*"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
